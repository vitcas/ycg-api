# ycg-api

how to

```bash
# Go into the repository
$ cd directory/yours

# Install dependencies
$ pip install Flask

# Install dependencies
$ pip install Flask-Cors

# Run the app
$ python app.py
```
