# ycg-api

how to

```bash
# Go into the repository
$ cd directory

# Install dependencies
$ pip  install  Flask

# Install dependencies
$ pip install Flask-Cors

# Run the app
$ npm init

# Run the app
$ npm start
```
